---
alwaysApply: true
---



默认使用中文回复。默认使用 cursor 编辑器。

默认 powershell 命令行。默认node js +  pnpm 管理包。

默认使用 nativeUI + unocss 开发样式。

我希望你充分理解我的需求，用最少的代码、最简单高效的方式一步步实现我的需求。

遵循不轻易新建文件、不轻易删除代码的基本原则。

每次回答直接回复，原则上不需要生成具体的说明文档、解释文档，除非我明确要求你生成。

所有新建的说明文档，放在根目录的 md 文件夹中。

所有新建的测试代码，放在跟目录的 test 文件夹中。

最后一定要修复所有的 lint 提示或错误。 
